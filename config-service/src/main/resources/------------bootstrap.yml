#Server port
server:
  port: 8888

spring:
  #程序名称
  application:
    name: config-service
  #Git repo location.
  cloud:
    config:
      label: master
      server:
        git:
          uri: https://github.com/JohnZero-US/config-server-repo
          search-paths:
          username:
          password: